ktor {
  application {
    modules = [
      org.lorenzo.modules.greeting.GreetingModuleKt.greeting,
      org.lorenzo.modules.graphql.GraphQLModuleKt.graphql,
    ]
  }
  deployment {
    port = 8080
    host = 0.0.0.0
  }
  database {
    driver = "org.postgresql.Driver",
    url = "jdbc:postgresql://localhost:5432/postgres",
    user = "postgres",
    password = "mysecretpassword",
  }
}